# ===========================================
# SandboxVTT Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values

# Environment (development or production)
NODE_ENV=development

# Server configuration
PORT=3001

# Client URL for CORS
CLIENT_URL=http://localhost:3000

# Database path
DATABASE_PATH=./server/database.sqlite

# Socket.io URL for client connections
# In development: Leave empty (uses Vite proxy)
# In production: Uses window.location.origin automatically
VITE_SOCKET_URL=

# ===========================================
# Production-specific settings
# ===========================================
# When deploying to production, also configure:

# SERVER_IP - Your server's IP address
# Example: SERVER_IP=123.45.67.89

# PUBLIC_CLIENT_PORT - Port users will access in their browser
# Example: PUBLIC_CLIENT_PORT=8080

# For production, update:
# - NODE_ENV=production
# - PORT=8080 (or your preferred port)
# - DATABASE_PATH=/var/www/sandboxvtt/server/database.sqlite
# - SERVER_IP and PUBLIC_CLIENT_PORT as shown above
